@model Slider
@{
    ViewData["Title"] = "Index";
}

<div class="container-fluid my-3 mx-3">
    <div class="table-responsive pt-3">
        <div class="create-btn mb-3">
            @*<a asp-area="AdminArea" asp-controller="Slider" asp-action="Create" class="btn btn-success"><i class="fa-solid fa-plus"></i></a>*@
        </div>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>
                        Image
                    </th>
                    <th style="text-align: center;">
                        Header
                    </th>

                    <th style="text-align: center;">
                        Title
                    </th>
                    <th style="text-align: center;">
                        Description
                    </th>

                </tr>
            </thead>
            <tbody>

                <tr>

                    <td>
                        <img src="~/assets/img/slider/@Model.Image" alt="Alternate Text" />
                    </td>
                    <td>
                        @Model.Header
                    </td>
                    <td>
                        @Model.Title
                    </td>
                    <td>
                        @Model.Description
                    </td>

                    @*<td style="text-align: center;">
            <a asp-action="Detail" asp-route-id="@Model.Id" type="button" class="btn btn-info"><i class="mdi mdi-information mx-0"></i></a>
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary"><i class="mdi mdi-table-edit"></i></a>
            <form style="display: inline-block;" asp-action="Delete" asp-route-id="@Model.Id" method="post">
                <button type="submit" class="btn btn-danger"><i class="mdi mdi-delete-forever"></i></button>
            </form>
        </td>*@
                </tr>

                }

            </tbody>
        </table>
    </div>
</div>
